# Movie-seat-booking

## querySelectorAll

## innerText

## addEventListener

## LocalStorage

- getItem()
- setItem()

## JSON

- JSON.parse()
- JSON.stringify()

## classList

- add()
- contains()
- toggle()

## spread

## Array.indexOf(

```js
seats.forEach((seat, index) => {
  if (selectedSeats.indexOf(index) > -1) {
    console.log('added');
    seat.classList.add('selected');
  }
});
```
